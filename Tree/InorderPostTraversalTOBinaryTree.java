package Tree;

public class InorderPostTraversalTOBinaryTree {

	  public TreeNode buildTree(int[] inorder, int[] postorder) {
	        TreeNode node = new TreeNode(0);
	        if(inorder.length < 0  || postorder.length <0)
	        	return node;
		  
		 
	  }
	
}
